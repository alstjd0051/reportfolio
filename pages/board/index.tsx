import React, { useEffect } from 'react';
import NavBar from '../../components/commons/layout/Nav';

type Props = {};

const BoardPage = (props: Props) => {
  return (
    <div>
      <NavBar />
    </div>
  );
};

export default BoardPage;
